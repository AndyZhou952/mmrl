# Docs site (MkDocs Material). Build locally with:
#   pip install -r requirements-docs.txt
#   mkdocs serve         # preview at http://127.0.0.1:8000
#   mkdocs build --strict
mkdocs-material>=9.5
mkdocs-same-dir>=0.1.3
